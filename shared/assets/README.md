# Shared Assets

This directory contains assets shared across all applications in the Compify monorepo.

## Structure

- `images/` - Shared images (logos, icons, etc.)
- `styles/` - Shared CSS/styling resources
- `fonts/` - Shared font files

## Usage

Applications should reference these assets using relative paths or build-time copying mechanisms to maintain independence while sharing common resources.